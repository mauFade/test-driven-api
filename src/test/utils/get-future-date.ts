import { setYear } from "date-fns";

/**
 * Função para retornar uma data com um ano para frente
 * @param date Data
 * @returns Data com um ano a mais
 */
export function setNextYear(date: string) {}
